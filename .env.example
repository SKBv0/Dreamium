# Dreamium Environment Configuration
# Copy this file to .env.local and fill in your values

# Application Settings
NEXT_PUBLIC_APP_NAME=Dreamium
NEXT_PUBLIC_APP_VERSION=1.0.0

# Secure Storage (REQUIRED)
# Generate a random 32-character string for encrypting API keys
# Used by lib/secure-store.ts for AES-256-GCM encryption
# Example: openssl rand -hex 16
KEYRING_SECRET=your-secret-key-here-change-this-to-random-32-chars

# AI Configuration (Optional)
# By default, Ollama runs on http://localhost:11434 (no configuration needed)
# OpenRouter API keys can be added via Settings UI (stored encrypted in localStorage)
